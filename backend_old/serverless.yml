service: rental-management-api

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  environment:
    PROPERTIES_TABLE: ${self:service}-properties-${self:provider.stage}
    TENANTS_TABLE: ${self:service}-tenants-${self:provider.stage}
    LEASES_TABLE: ${self:service}-leases-${self:provider.stage}
    PAYMENTS_TABLE: ${self:service}-payments-${self:provider.stage}
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
      Resource: "arn:aws:dynamodb:${self:provider.region}:*:table/*"

functions:
  properties:
    handler: src/handlers/properties.handler
    events:
      - http:
          path: properties
          method: any
          cors: true

  tenants:
    handler: src/handlers/tenants.handler
    events:
      - http:
          path: tenants
          method: any
          cors: true

  leases:
    handler: src/handlers/leases.handler
    events:
      - http:
          path: leases
          method: any
          cors: true

  payments:
    handler: src/handlers/payments.handler
    events:
      - http:
          path: payments
          method: any
          cors: true

resources:
  Resources:
    PropertiesTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.PROPERTIES_TABLE}
        AttributeDefinitions:
          - AttributeName: propertyId
            AttributeType: S
        KeySchema:
          - AttributeName: propertyId
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST

    TenantsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.TENANTS_TABLE}
        AttributeDefinitions:
          - AttributeName: tenantId
            AttributeType: S
        KeySchema:
          - AttributeName: tenantId
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST

    LeasesTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.LEASES_TABLE}
        AttributeDefinitions:
          - AttributeName: leaseId
            AttributeType: S
        KeySchema:
          - AttributeName: leaseId
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST

    PaymentsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.PAYMENTS_TABLE}
        AttributeDefinitions:
          - AttributeName: paymentId
            AttributeType: S
        KeySchema:
          - AttributeName: paymentId
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST